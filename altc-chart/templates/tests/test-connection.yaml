apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "altc-chart.fullname" . }}-test-connection"
  labels:
    {{- include "altc-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "altc-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
